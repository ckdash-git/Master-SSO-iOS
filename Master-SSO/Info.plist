<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>

    <!-- ── Bundle identity ─────────────────────────────────────────────── -->
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>

    <!-- ── MSAL broker redirect URI scheme ─────────────────────────────── -->
    <!--
        MSAL broker format: msauth.<bundle-id>://auth
        Register this in Azure AD → App Registration → Authentication →
        Add a platform → iOS/macOS, entering Bundle ID: com.cachatto.Master-SSO
        Azure auto-generates the redirect URI msauth.com.cachatto.Master-SSO://auth
    -->
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key>
            <string>com.cachatto.Master-SSO.auth</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>msauth.com.cachatto.Master-SSO</string>
            </array>
        </dict>
        <!-- ── Google OAuth callback scheme ──────────────────────────────── -->
        <!--
            Reversed client ID from Google Cloud Console →
            APIs & Services → Credentials → your iOS OAuth 2.0 Client ID.
            Replace with the actual reversed client ID for your credentials.
            e.g. com.googleusercontent.apps.123456789-abcdefghij
        -->
        <dict>
            <key>CFBundleURLName</key>
            <string>com.googleusercontent.apps</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>com.googleusercontent.apps.541572207213-mus0961fn7f5pv26ih1qvorvd5cdiibk</string>
            </array>
        </dict>
    </array>

    <!-- ── canOpenURL whitelist ─────────────────────────────────────────── -->
    <!--
        iOS 9+ requires schemas queried with canOpenURL to be declared here.
        Microsoft:
          "msteams"         — Microsoft Teams
          "ms-outlook"      — Microsoft Outlook
          "msauthv2"        — Microsoft Authenticator broker (MSAL v2)
          "msauthv3"        — Microsoft Authenticator broker (MSAL v3)
        Google:
          "googlegmail"     — Gmail
          "meet"            — Google Meet
          "googledrive"     — Google Drive
          "googlecalendar"  — Google Calendar
    -->
    <key>LSApplicationQueriesSchemes</key>
    <array>
        <string>msteams</string>
        <string>ms-outlook</string>
        <string>msauthv2</string>
        <string>msauthv3</string>
        <string>googlegmail</string>
        <string>meet</string>
        <string>googledrive</string>
        <string>googlecalendar</string>
    </array>

    <!-- ── Interface ───────────────────────────────────────────────────── -->
    <key>UILaunchScreen</key>
    <dict/>

    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
    </dict>

    <key>UIApplicationSupportsIndirectInputEvents</key>
    <true/>

    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

</dict>
</plist>
